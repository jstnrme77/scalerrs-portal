# Use legacy peer deps to avoid issues with Next.js and React
legacy-peer-deps=true

# Disable binary downloads that might cause issues in Bolt
ignore-scripts=true

# Use npm registry
registry=https://registry.npmjs.org/
